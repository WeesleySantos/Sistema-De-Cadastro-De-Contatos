@model SiteWeb.Models.HomeViewModel
@{
    ViewData["Title"] = "Home Page";
}

<div class="text-center">
    <h1 class="display-4">Bem vindo, @Model.Nome</h1>
    <p>Meu Site<p>
        <span>Seu e-mail é: @Model.Email</span>
</div>
